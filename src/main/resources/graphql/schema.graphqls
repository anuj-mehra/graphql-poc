scalar Long

type Book {
    id: Long
    title: String
    author: String
    pages: Int
}

type Query {
    getAllBooks: [Book]
    getBookById(id: Long): Book
}

type Mutation {
    addBook(title: String, author: String, pages: Int): Book
}
